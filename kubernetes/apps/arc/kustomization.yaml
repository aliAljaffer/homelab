---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - arc-namespaces.yaml
  # arc-runner-set-values.yaml is used by the ARC ArgoCD Application helm source
  # Uncomment after sealing:
  # - github-token.sealed.yaml
