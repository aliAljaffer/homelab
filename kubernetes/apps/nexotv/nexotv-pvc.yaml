apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexotv-data
  namespace: stremio
spec:
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
  accessModes: [ "ReadWriteOnce" ]
