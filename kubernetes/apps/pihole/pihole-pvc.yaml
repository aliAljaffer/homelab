apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-pvc
  namespace: pihole
spec:
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
  accessModes: ["ReadWriteOnce"]
