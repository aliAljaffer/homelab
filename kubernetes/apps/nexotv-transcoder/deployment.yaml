apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexotv-transcoder
  namespace: stremio
  labels:
    app: nexotv-transcoder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nexotv-transcoder
  template:
    metadata:
      labels:
        app: nexotv-transcoder
    spec:
      volumes:
      - name: nexotv-transcoder-src
        configMap:
          name: nexotv-transcoder-src
      containers:
      - name: transcoder
        image: node:20-slim
        command: ["sh", "-c", "npm install express && node /app/transcoder.js"]
        workingDir: /app
        ports:
        - containerPort: 7001
        env:
        - name: PORT
          value: "7001"
        volumeMounts:
        - name: nexotv-transcoder-src
          mountPath: /app/transcoder.js
          subPath: transcoder.js
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: "1"
            memory: 512Mi
