---
# Longhorn requires privileged containers (block storage needs raw device access).
# Label the namespace to exempt it from PodSecurity baseline enforcement.
apiVersion: v1
kind: Namespace
metadata:
  name: longhorn-system
  labels:
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/enforce-version: latest
