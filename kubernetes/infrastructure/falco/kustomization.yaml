---
# Extra resources deployed alongside the falco Helm chart
# (referenced by the falco ArgoCD Application multi-source)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
