apiVersion: apps/v1
kind: Deployment
metadata:
  name: snapshot-controller
  namespace: kube-system
spec:
  template:
    spec:
      containers:
        - name: snapshot-controller
          resources:
            requests:
              cpu: 5m
              memory: 56Mi
            limits:
              cpu: 20m
              memory: 112Mi
